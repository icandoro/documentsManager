security:
  password_hashers:
    App\Entity\User:
      algorithm: auto

  providers:
    app_user_provider:
      id: App\Security\DatabaseUserProvider

  firewalls:
    dev:
      pattern: ^/(_(profiler|wdt)|css|images|js)/
      security: false
    api:
      pattern: ^/api
      stateless: true
      provider: app_user_provider
      custom_authenticators:
        - App\Security\ApiTokenAuthenticator
      json_login:
        check_path: /api/auth/login
        username_path: email
        password_path: password
      # Next step: implement API token issue/refresh and 2FA challenge endpoints.

  access_control:
    - { path: ^/api/auth, roles: PUBLIC_ACCESS }
    - { path: ^/api/ocr/identity, roles: PUBLIC_ACCESS }
    - { path: ^/api/public, roles: PUBLIC_ACCESS }
    - { path: ^/api/health, roles: PUBLIC_ACCESS }
    - { path: ^/api/platform-admin, roles: [ROLE_SUPER_ADMIN, ROLE_ADMIN, ROLE_PLATFORM_ADMIN] }
    - { path: ^/api, roles: ROLE_USER }
