pipeline {
    agent any

    stages {
        stage('Test Jenkins') {
            steps {
                echo 'Jenkins functioneaza pentru documentsManager'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}